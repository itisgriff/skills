interface:
  display_name: "SQLite Data"
  short_description: "Use when working with SQLiteData, StructuredQueries, or GRDB in Swift — database models, queries, reactive fetching, mutations, migrations, and CloudKit sync."
  brand_color: "#1A73E8"
  default_prompt: "Use $sqlite-data to review my project."

policy:
  allow_implicit_invocation: true
